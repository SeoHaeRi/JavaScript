Array(100)
    .fill(0)
    .map((value, index) => (index + 1) ** 2);

let sum = 0;
"12367"
    .split("")
    .map((value) => parseInt(value))
    .forEach((value) => (sum += value));

let today = new Date("2021/12/5/12:00");
today.toISOString();
